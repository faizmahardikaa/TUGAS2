HOST=127.0.0.1
PORT=3000
DEMO_USERNAME=mahasiswa
# npm run setup generates .env with a bcrypt hash and two independent random keys.
PASSWORD_HASH=GENERATE_WITH_SETUP
JWT_SECRET=GENERATE_WITH_SETUP
AES_KEY_HEX=GENERATE_WITH_SETUP
